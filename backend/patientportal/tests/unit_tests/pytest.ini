[pytest]
pythonpath = ../../
filterwarnings =
    # We ignore deprecation warnings from the `pydantic` library
    ignore::DeprecationWarning:pydantic.*
    # We ignore deprecation warnings from the `jose` library
    ignore::DeprecationWarning:jose.*


